// main.cpp: hienpdbk
// Description: Template used to solve CP.

#include <iostream>
#include <vector>
#include <set>
#include <algorithm>

using namespace std;

int solve(int value) {
    return value;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int input;
    cin >> input;
    cout << solve(input) << "\n";
    return 0;
}
